spring:
  profiles: test
  datasource:
    initialization-mode: always
    platform: postgres
    url: "jdbc:postgresql://localhost:5432/e2etest"
    username: testuser
    password: postgres
  liquibase:
    drop-first: true